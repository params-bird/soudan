.mobile-body
  =render 'shared/header'
  .mobile-body-fixe-flame
    .mobile-body__inner
      =render partial: 'topic-box', collection: @topic, as: :topic
  - if user_signed_in?
    -# =render 'shared/footer'