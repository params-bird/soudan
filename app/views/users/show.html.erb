<p id="notice"><%= notice %></p>

<h1>ユーザー詳細ページ</h1>
<h3><%= @user.name %></h3>
<h2>投稿内容</h2>
<% @user.products.each do |product| %>
  <a href="#<%= product.user.id %>"><%= product.introduction %></a>
  <hr>
<% end %>
<h3>いいねしている投稿</h3>
<% @user.liked_products.each do |product| %>
  <%= product.user.name %>
  <%= product.introduction %>
<% end %>

    <p><a href="/rooms">受信BOXへ</a>


<%# ------------------------------------------------ %>

<% if @user.id == current_user.id %>

<% else %>
  <% if @is_room == true %>
    <p><a href="/rooms/<%= @room_id %>">チャットへ</a>
    <% else %>
    <%= form_for @room do |f| %>
      <%= fields_for @entry do |e| %>
        <%= e.hidden_field :user_id, value: @user.id %>
      <% end %>
      <%= f.submit "チャットを始める" %>
    <% end %>
  <% end %>
<% end %>


<%= link_to "ユーザー一覧へ", users_path %>
<%= link_to "ホームへ戻る", products_path %>

<p>
  <strong>Name:</strong>
    <%= @user.name %>

</p>

 <%
=begin%>
 <section class="stats">
  <%= render 'shared/stats' %>
  <%= render 'follow_form' if user_signed_in? %>
</section>
<%
=end%>

<%
=begin%>
 <p>
  <strong>Avater:</strong>
  <%= @user.avater %>
</p>
<%
=end%>

<%= link_to 'Edit', edit_user_registration_path(@user) %> |
<%= link_to 'Back', root_path %>